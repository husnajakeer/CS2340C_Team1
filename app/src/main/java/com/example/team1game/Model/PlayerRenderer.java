package com.example.team1game.Model;

/**
 * this file would be used if we did "render playerMovement using observer pattern"
 * maybe don't work on this for now
 */
public class PlayerRenderer implements Subscriber{

    @Override
    public void update(Subscriber subscriber) {
        // draw()
    }
}
